import { Card, CardContent, CardHeader, CardTitle } from '../../ui/card';

export default function UserReview() {
  return (
    <Card>
      <CardHeader>
        <CardTitle></CardTitle>
      </CardHeader>
      <CardContent>
        <div></div>
      </CardContent>
    </Card>
  );
}
